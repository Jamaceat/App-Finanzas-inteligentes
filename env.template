CODIGO_LAN=11111

DIRECCION_IP_DISPOSITIVO=192.168.X.X

# Puerto del diálogo "Vincular dispositivo con código de vinculación" (solo para adb pair).
PUERTO=3000

# Puerto que se ve en la pantalla principal de "Depuración inalámbrica" (para adb connect).
# Es distinto al PUERTO de arriba y suele cambiar cada vez que se reconecta el WiFi.
PUERTO_DEPURACION=3000

